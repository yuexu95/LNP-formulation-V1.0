# Streamlit secrets. Values are encrypted at rest and served securely at runtime.
# Update here and wait ~1 minute for changes to propagate.

[default]
# Example service credentials
#api_base_url = "https://api.example.com"
#api_key = "replace-with-api-key"

[database]
#url = "postgresql://user:password@host:5432/dbname"
# Optional pool settings
#max_connections = 5
#timeout = 60

[email]
#smtp_server = "smtp.mailprovider.com"
#smtp_port = 587
#smtp_username = "user@example.com"
#smtp_password = "replace-with-password"
#use_tls = true

# Usage in app (Python):
# import streamlit as st
# api_key = st.secrets["default"]["api_key"]
# db_url = st.secrets["database"]["url"]
